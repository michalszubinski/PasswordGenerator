#pragma once

#include "PasswordGenerator_Libraries.h"

static class PasswordGenerator_Utils 
{
public:
	static unsigned int countSpecificLetters(const std::string text, const std::string letters);
};